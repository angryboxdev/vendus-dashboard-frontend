# API do backend. Em dev (vite) deixe vazio para usar o proxy (localhost:3333).
# Em produção use o URL do backend, ex.: https://vendus-dashboard-backend.onrender.com
# Pode usar VITE_API_BASE_URL ou VITE_API_URL (o código aceita os dois; prioridade: BASE).
# VITE_API_BASE_URL=http://localhost:3333
# VITE_API_URL=
